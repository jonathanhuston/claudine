Hi Claude. I'd like us to start a CLI project in Swift together. Let's plan it carefully first before writing any code.

I've been thinking about how software development has been progressing, with you (Claude) doing the bulk of the actual coding work while the human software developer (in this case, me) specifies the project, continuously tweaks the direction and implementation, evaluates and edits code, and tests it in the real world. Is that roughly how you understand AI-assisted/AI-driven software development to work, at least for straightforward, manageable, non-corporate, one-person projects?

So normally, we've been coding in established programming languages like Python, JavaScript, or in our case also Clojure, Gleam, Elixir... that were generally developed long before coding agents like Claude Code came along. I'm thinking it could be useful to create a simple, dedicated, interpreted language (implemented in Swift) for writing CLI tools with Claude Code. (Later on, we might want to expand this to GUI and web apps, etc., but for now, let's stick to simple CLI apps – we might want to keep the expansion option in mind, though.) 

The main goals are: 1) Syntax and logic that make it straightforward for you (Claude) to write error-free, efficient code, and to create a complete test suite – keeping in mind that, unlike Python or Elixir, there is no existing code base you can draw on. 2) Syntax and logic that make it straightforward for me (the human programmer) to understand, edit, and modify the code. 3) Built-in ability to seemlessly integrate API calls to Claude within the app, which generate more flexible, albeit probabilistic, reactions to user input, etc. – i.e. we want to leverage modern AI capabilities within the app, with low coding overhead. 4) As mentioned, this will be an interpreted language (at least for now), so it would be great to have a CLI REPL that I (the human programmer) can use to test code on the fly. 5) I'm not a big fan of Python-like indents, so we'll use a different block structure that doesn't depend on formatting. 6) I'm agnostic as to whether we implement OOP, functional, imperative or other programming style – whatever you think makes the most sense for implementing the other goals. 7) Once the language is implemented, let's create a VS Code extension for syntax highlighting, etc. 8) Let's call the language "Claudine", for obvious reasons.

